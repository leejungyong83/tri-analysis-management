name: tri_inspection_app
description: "TRI 공정 후 표면 시약 검사 기록 앱 (Android APK / iOS PWA)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.1.0
  connectivity_plus: ^6.0.0
  uuid: ^4.4.0
  image: ^4.2.0
  url_launcher: ^6.3.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
